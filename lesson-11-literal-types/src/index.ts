// literal types are a feature that allows you to specify exact, specific values as types, rather than general categories like string or number.

type Quantity = 50 | 100
let quantity: Quantity = 50

type Metric = 'cm' | 'inch'
let metric: Metric = 'cm'